{
  "name": "form-check-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^15.2.11",
    "prettier": "^3.7.1"
  },
  "lint-staged": {
    "frontend/**/*.{js,jsx,ts,tsx,css,md,json}": [
      "prettier --write"
    ],
    "backend/**/*.py": [
      "backend/venv/bin/ruff format"
    ]
  }
}
